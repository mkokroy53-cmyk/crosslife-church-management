﻿# Church Pro .htaccess

# Optimized for Apache 2.4+
<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase "/church pro/"
</IfModule>

# Block access to sensitive files
<FilesMatch "\.(env|log|sql|md)$">
    Require all denied
</FilesMatch>

